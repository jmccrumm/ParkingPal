From: <%= @message.participants[1].name %><br>
Subject: <%= @message.subject %><br>

Body:
<% receipt = @message.receipts_for(current_user)[0] %>
<%= receipt.message.body %>

<% if false %>
	This section is DELEBERATLY 'commented' out, this 
	each, would allow to display multiple messages associated with this
	conversation
	<% @message.receipts_for(current_user).each do |receipt| %>
		<%= receipt.message.body %>
	<% end %>
<% end %>
