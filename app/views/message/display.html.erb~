From: <%= @message.participants[1].name %><br>
Subject: <%= @message.subject %><br>
Last update: <%= @message.updated_at.strftime("%-d %B %Y, %H:%M:%S") %><br>

<br>
<% receipt = @message.receipts_for(current_user)[0] %>

	<% @message.receipts.each do |receipt| %>
			<% if @duplicated %>
				<%= receipt.receiver.name %>: 
				<%= receipt.message.body %>
				<br>		<br>
			<% end %>
			<% @duplicated = !@duplicated; %>
	<% end %>

<br><br>
<%= link_to "Reply", message_reply_path(msg: @message) %>
