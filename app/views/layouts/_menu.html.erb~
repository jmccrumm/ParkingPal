<meta charset="utf-8">
  <meta content="width=device-width, initial-scale=1.0" name="viewport"/>
  <title>@ViewBag.Title - My ASP.NET Application</title>
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet"></link>
  <body></body>
  <div class="navbar">
    <ul class="container">
      <h1>Parking Pal</h1>
      <a href="<%= root_path %>">
        <li>Home</li>
      </a>
      <% if user_signed_in? %>
          <%= link_to "<li>Logout</li>".html_safe, destroy_user_session_path, :method => :delete %>
      <% else %>
        <a href="<%= user_path %>">
          <li>Login / Signup</li>
        </a>
      <% end %>
      <i class="material-icons">error_outline</i>
    </ul>
  </div>
  <div class="col-1 menu">
    <div style="display: inline"></div>
    <div>
      <a href="<%= parkinglot_path %>">
        <i class="material-icons" style="background-color: aqua; font-size: 3em" title="Map">map</i>
      </a>
      <a href="<%= user_path %>">
        <i class="material-icons" style="background-color: magenta; font-size: 3em" title="Account">account_circle</i>
      </a>
      <a href="<%= message_path %>">
        <i class="material-icons" style="background-color: aquamarine; font-size: 3em" title="Messages">question_answer</i>
      </a>
      <a href="<%= stats_path %>">
        <i class="material-icons" style="background-color: orange; font-size: 3em" title="Stats">timeline</i>
      </a>
    </div>
  </div>
</meta>
<br/>


<% if current_page?(message_path) || current_page?(message_display_path) || current_page?(message_new_path) %>
  <%= link_to("Inbox", message_path) %>
	<% if false %>
  	<%= link_to("Link to test message", message_display_path) %>
	<% end %>
  <%= link_to("Compose", message_compose_path) %>
  <br/>
<% end %>

<% if current_page?(user_path) %>
  <%= link_to("Schedule", schedule_path) %>
  <br/>
<% end %>

<% if current_page?(schedule_path) %>
  <%= link_to("< Back to Account", user_path) %>
  <%= link_to("Change Schedule", schedule_modify_path) %>
  <br/>
<% end %>

<% if current_page?(schedule_modify_path) %>
  <%= link_to("< Back to Schedule", schedule_path) %>
  <br/>
<% end %>
