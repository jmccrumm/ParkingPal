// Place all the styles related to the parkingspot controller here.
// They will automatically be included in application.css.
// You can use Sass (SCSS) here: http://sass-lang.com/

#spotInfo {
	padding: 2ex;
	position: absolute;
	border: 2px double black;
	
	background: radial-gradient(#B0C4DE, #5F9EA0, #2F4F4F); 
}

p {
	font-family: Georgia, serif;
	font-variant: small-caps;
	font-weight: 900;
	align: center;
}


p.open {

	color: green;
}
p.taken {
	color: red;
}
p.reserved {
	color: yellow;
}

.notice {
	width:600px;
	background-color: green;
	border: 1px solid rgb(45, 210, 45);
	border-radius:25px;
	padding: 15px;
	margin: 15px;
	text-align:center;
	text-color: black;
}
